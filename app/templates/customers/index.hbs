<Page::Header @title="Customers" />
<Page::Content>
  <div class="d-flex justify-content-between align-content-center mb-3 form-inline">
    <h5 class="card-title">
      {{this.sortedCustomers.length}} Results
    </h5>
    <div>
      <select class="form-control" onchange={{action (mut this.sortProperty) value="target.value" }}>
        <option value="fullName" selected={{eq this.sortProperty "fullName" }}>
          Sort by name
        </option>
        <option value="budget" selected={{eq this.sortProperty "budget" }}>
          Sort by budget
        </option>
      </select>
    </div>
  </div>
  <div class="card">
    <CustomersTable @model={{this.sortedAndFilteredCustomers}} />
  </div>
  <div class="form-row mt-3">
    <div class="form-select col-md-4">
      <select class="form-control" onchange={{action (mut this.SearchProperty) value="target.value" }}>
        <option value="fullName" selected={{eq this.SearchProperty "fullName" }}>
          Name
        </option>
        <option value="company" selected={{eq this.SearchProperty "company" }}>
          Company
        </option>
        <option value="project" selected={{eq this.SearchProperty "project" }}>
          Project
        </option>
      </select>
    </div>
    <div class="form-group col-md-6">
      <Input @placeholder="Seach Customers" @type="text" @class="form-control" @id="search" onchange={{action (mut this.SearchString) value="target.value"}}/>
      <small class="form-text text-muted">Click off of it to apply search</small>
    </div>
  </div>
</Page::Content>